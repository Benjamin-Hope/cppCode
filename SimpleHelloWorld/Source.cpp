#include<iostream> //this includes the labrary for input and output

/*multi line
comments*/

int main() { //main function 
	//the code will be run sequentially 
	std::cout << "Hello World\n"; //std::count is a character output stream
	
}

/*To complite the code in the terminal we can write
"g++ {filename}.cpp" and to execute wirite after "./a.out"*/

/*We can name the file when compiling '#kinda like alias', by doing
g++ {filename}.cpp -o {alias} and then run with just "./{alias}*/